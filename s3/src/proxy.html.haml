<!DOCTYPE html>
%html{ :lang => "en" }
  %head
    %meta{ :charset => 'utf-8' }
    %title S3 proxy

    %script{ :src => "proxy.js", :type => "text/javascript" }
  %body
    :javascript
      window.onload = function() {
        new Proxy.Proxy ({
          end_point:    parent,
          from:         "http://application.dev", // TODO your application's domain
          worker_class: Proxy.ImageLoader
        })
      }
